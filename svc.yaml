apiVersion: v1
kind: Service
metadata:
  name: weather-service
  labels:
    app: weather
spec:
  type: NodePort
  selector:
    app: weather
    version: blue
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30000
      protocol: TCP
